[build]
builder = "nixpacks"

[[services]]
name = "api"
root = "backend"
[services.deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30

[[services]]
name = "web"
root = "frontend"
[services.build]
cmd = "npm run build"
[services.deploy]
startCommand = "npm start"

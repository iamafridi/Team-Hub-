{
  "name": "team-hub",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.2.3",
  "description": "Collaborative Team Hub — Full-stack web application",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "start": "npm run -w @team-hub/api start",
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}

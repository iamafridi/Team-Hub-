{
  "name": "team-hub",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.2.3",
  "description": "Collaborative Team Hub — Full-stack web application",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
